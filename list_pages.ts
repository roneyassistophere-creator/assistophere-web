import { config } from 'dotenv'; config(); import payloadConfig from './src/payload.config'; import { getPayload } from 'payload'; async function run() { const payload = await getPayload({ config: payloadConfig }); const pages = await payload.find({ collection: 'pages', limit: 100, depth: 0 }); console.log('All Pages:', pages.docs.map(p => ({title: p.title, slug: p.slug}))); process.exit(0); } run();